name: "Hello world workflow"

# Controld when the actions will run. Triggers the workflows on push to master and production branches
on:
  push:
    branches: 
      - master
      - production

jobs:
   hello_world:
     runs-on: ubuntu-latest
     steps:
       - name: "Print hello world"
         run: echo "Hello world"